# my-startup-app

Enterprise-grade fullstack starter scaffold (React + Vite + TypeScript frontend, Node + Express + TypeScript backend, Prisma + MySQL, Redis, Socket.IO, Docker + Nginx).

See the `frontend/` and `backend/` folders for entry points and setup.
